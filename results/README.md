# Analysis outputs

The scripts write small tabular results, figures and enrichment outputs under this directory.

Before public release, commit only compact final outputs that are useful for reproducibility. Large intermediate objects belong in `data/` and are ignored.
